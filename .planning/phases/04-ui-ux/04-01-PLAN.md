---
phase: 04-ui-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - easter-egg-mint/src/components/App.tsx
  - easter-egg-mint/src/components/screens/ConnectScreen.tsx
  - easter-egg-mint/src/components/screens/GenerateScreen.tsx
  - easter-egg-mint/src/app/page.tsx
  - easter-egg-mint/src/hooks/useEggFlow.ts
autonomous: true
requirements: [R6]

must_haves:
  truths:
    - "App shell renders with safe area insets from Farcaster SDK context"
    - "User auto-connects wallet on load (Warpcast farcasterFrame or Base App coinbaseWallet)"
    - "Manual Connect button appears after 3s if auto-connect fails"
    - "After connect, user sees their PFP and a Generate button"
    - "Clicking Generate shows two-phase loading: trait extraction then egg generation"
    - "State machine drives screen transitions (no URL routing)"
  artifacts:
    - path: "easter-egg-mint/src/hooks/useEggFlow.ts"
      provides: "State machine hook managing connect->generate->preview->mint->success flow"
      exports: ["useEggFlow"]
    - path: "easter-egg-mint/src/components/App.tsx"
      provides: "Root client component with safe area insets and screen routing"
      min_lines: 40
    - path: "easter-egg-mint/src/components/screens/ConnectScreen.tsx"
      provides: "Wallet connection screen with auto-connect + manual fallback"
      min_lines: 60
    - path: "easter-egg-mint/src/components/screens/GenerateScreen.tsx"
      provides: "PFP display, generate button, two-phase loading states"
      min_lines: 80
  key_links:
    - from: "easter-egg-mint/src/components/App.tsx"
      to: "useEggFlow"
      via: "hook drives which screen renders"
      pattern: "useEggFlow"
    - from: "easter-egg-mint/src/components/screens/ConnectScreen.tsx"
      to: "wagmi useConnect"
      via: "auto-connect + manual connect"
      pattern: "useConnect|connect\\("
    - from: "easter-egg-mint/src/components/screens/GenerateScreen.tsx"
      to: "/api/extract-traits, /api/generate-egg"
      via: "sequential fetch calls"
      pattern: "fetch.*api/(extract-traits|generate-egg)"
---

<objective>
Build the app shell, state machine, and first two screens (Connect + Generate) for the Easter Egg Mint flow.

Purpose: Establish the single-page state machine architecture and get users from wallet connection through egg generation, matching main-apostole vintage aesthetic.
Output: Working Connect and Generate screens with state transitions, safe area insets, loading states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux/04-RESEARCH.md

@easter-egg-mint/src/app/layout.tsx
@easter-egg-mint/src/app/providers.tsx
@easter-egg-mint/src/app/globals.css
@easter-egg-mint/tailwind.config.ts
@easter-egg-mint/src/hooks/useMintEgg.ts
@easter-egg-mint/src/lib/constants.ts
@easter-egg-mint/src/lib/config.ts
@easter-egg-mint/src/lib/traits.ts
@easter-egg-mint/src/app/api/extract-traits/route.ts
@easter-egg-mint/src/app/api/generate-egg/route.ts
@main-apostole/src/components/SignInScreen.tsx
@main-apostole/src/components/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: State machine hook + App shell</name>
  <files>
    easter-egg-mint/src/hooks/useEggFlow.ts
    easter-egg-mint/src/components/App.tsx
    easter-egg-mint/src/app/page.tsx
  </files>
  <action>
    **useEggFlow.ts** — Create a state machine hook that manages the full app flow:
    - States: "connect" | "generate" | "preview" | "minting" | "success" | "error"
    - Stored data: `traits` (EggTraits | null), `imageBase64` (string | null), `txHash` (string | null), `tokenURI` (string | null), `imageURI` (string | null), `error` (string | null)
    - Transitions: `onConnected()`, `onGenerated(traits, imageBase64)`, `onMetadataUploaded(tokenURI, imageURI)`, `onMinted(txHash)`, `onError(msg)`, `reset()`
    - Use `useState` (not useReducer — keep it simple, this is a linear flow)
    - Export the hook and the `FlowState` type

    **App.tsx** — Create root client component (adapt from main-apostole App.tsx pattern):
    - `"use client"` directive
    - Use `useMiniApp()` from `@neynar/react` for SDK context and safe area insets
    - Apply safe area insets via inline style on root div (top, bottom, left, right from `context.client.safeAreaInsets`)
    - Use `useEggFlow()` to determine which screen to render
    - Switch on `state`: "connect" renders ConnectScreen, "generate" renders GenerateScreen, others render placeholder divs for now
    - One-time `sdk.actions.addMiniApp()` call (copy pattern from main-apostole App.tsx)
    - Root div: `min-h-screen bg-paper flex flex-col`
    - Call `sdk.actions.ready()` once SDK is loaded

    **page.tsx** — Replace current placeholder with dynamic import of App component:
    - Keep the metadata generation (generateMetadata) as-is
    - Default export: dynamically import App with `{ ssr: false }` using `next/dynamic`
    - The page component just renders `<App />`

    Use `/frontend-design` skill reference pattern: vintage aesthetic with bg-paper, text-vintage-black, Old Standard TT font, border-2 border-vintage-black on cards/containers.
  </action>
  <verify>
    `cd easter-egg-mint && npx tsc --noEmit` passes with no type errors in the new files.
  </verify>
  <done>
    useEggFlow hook exports all state transitions. App.tsx renders with safe area insets. page.tsx dynamically imports App. State machine starts at "connect" state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Connect and Generate screens</name>
  <files>
    easter-egg-mint/src/components/screens/ConnectScreen.tsx
    easter-egg-mint/src/components/screens/GenerateScreen.tsx
  </files>
  <action>
    **ConnectScreen.tsx** — Adapt directly from main-apostole's SignInScreen.tsx:
    - Props: `onConnected: () => void`
    - Use `useMiniApp()` for context, `useAccount()` + `useConnect()` from wagmi
    - Auto-connect logic: detect farcasterFrame vs coinbaseWallet connector (Base App clientFid = 309857)
    - Try auto-connect on mount; show spinner during attempt
    - After 3 seconds, show manual "CONNECT WALLET" button if not connected
    - When connected, call `onConnected()`
    - Show Farcaster username from `context.user.username` when available
    - Styling: full-screen bg-paper, centered content, button is `w-full py-4 border-2 border-vintage-black bg-vintage-black text-paper font-bold text-lg tracking-widest`
    - Spinner: `w-8 h-8 border-2 border-vintage-black border-t-transparent rounded-full animate-spin`
    - Title: "Easter Egg Mint" in text-4xl font-bold, subtitle: "Generate a unique egg from your PFP"

    **GenerateScreen.tsx** — New screen showing PFP and triggering generation:
    - Props: `onGenerated: (traits: EggTraits, imageBase64: string) => void`, `onError: (msg: string) => void`
    - Use `useMiniApp()` to get `context.user` for PFP URL and display name
    - Use `useAccount()` to get connected address (for fid fallback)
    - Display user's PFP in a 120x120 rounded-full border-2 border-vintage-black container
    - Show username below PFP
    - "GENERATE YOUR EGG" button (same vintage button style as ConnectScreen)
    - On click, set loading state and make two sequential fetches:
      1. POST `/api/extract-traits` with `{ pfpUrl: context.user.pfpUrl, fid: context.user.fid }` — show "Analyzing your PFP..." loading text
      2. POST `/api/generate-egg` with `{ fid: context.user.fid, traits: extractedTraits }` — show "Creating your egg..." loading text
    - On success, call `onGenerated(traits, imageBase64)`
    - On error, call `onError(errorMessage)`
    - Loading state: replace button with spinner + phase-appropriate text, disable interaction
    - Error state: show error message in text-error with "Try Again" button

    Use `/frontend-design` skill when building these screens. Mobile-first layout, max-w-md container, centered content.
  </action>
  <verify>
    `cd easter-egg-mint && npx tsc --noEmit` passes. Both screen components export correctly and are imported in App.tsx.
  </verify>
  <done>
    ConnectScreen auto-connects wallet and falls back to manual button. GenerateScreen shows PFP, triggers trait extraction + egg generation with two-phase loading, and transitions to preview state on success.
  </done>
</task>

</tasks>

<verification>
- `cd easter-egg-mint && npx tsc --noEmit` — no type errors
- `cd easter-egg-mint && npm run build` — builds successfully
- App.tsx imports and renders ConnectScreen and GenerateScreen based on useEggFlow state
- page.tsx uses dynamic import with ssr: false
</verification>

<success_criteria>
- State machine hook manages connect -> generate -> preview flow
- ConnectScreen mirrors main-apostole's auto-connect + manual fallback pattern
- GenerateScreen calls extract-traits then generate-egg APIs with two-phase loading
- Safe area insets applied from Farcaster SDK context
- Vintage aesthetic (bg-paper, Old Standard TT, vintage-black borders) consistent throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-ux/04-01-SUMMARY.md`
</output>
